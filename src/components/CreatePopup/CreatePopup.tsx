import { observer } from 'mobx-react-lite'
import React from 'react'
import styles from './CreatePopup.module.scss'

const CreatePopup = observer(() => {
  return (
    <div className={styles.wrapper}>CreatePopup</div>
  )
})

export default CreatePopup