import { observer } from 'mobx-react-lite'
import styles from './CreateTaskPopup.module.scss'

const CreateGroupPopup: React.FC = observer(() => {
  return (
    <div className={styles.wrapper}></div>
  )
})

export default CreateGroupPopup